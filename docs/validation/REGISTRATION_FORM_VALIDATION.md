# Валидация формы регистрации

## Внесенные улучшения

### 1. **Валидация имени пользователя**
- ✅ **Обязательность ввода**: Поле не может быть пустым
- ✅ **Минимальная длина**: Минимум 3 символа
- ✅ **Сообщения об ошибках**:
  - "Имя пользователя обязательно для заполнения"
  - "Имя пользователя должно содержать минимум 3 символа"

### 2. **Валидация email**
- ✅ **Обязательность ввода**: Поле не может быть пустым
- ✅ **Формат email**: Проверка с помощью регулярного выражения `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
- ✅ **Сообщения об ошибках**:
  - "Email обязателен для заполнения"
  - "Введите корректный email адрес"

### 3. **Валидация пароля**
- ✅ **Обязательность ввода**: Поле не может быть пустым
- ✅ **Минимальная длина**: Минимум 8 символов
- ✅ **Подтверждение пароля**: Добавлено второе поле для подтверждения
- ✅ **Проверка совпадения**: Пароли должны совпадать
- ✅ **Показ/скрытие пароля**: Переключатель для каждого поля пароля
- ✅ **Сообщения об ошибках**:
  - "Пароль обязателен для заполнения"
  - "Пароль должен содержать минимум 8 символов"
  - "Подтвердите пароль"
  - "Пароли не совпадают"

### 4. **Визуальная индикация ошибок**
- ✅ **Подсветка полей с ошибками**: Красная рамка и фон
- ✅ **Сообщения под полями**: Текстовое описание ошибки
- ✅ **Анимация появления**: Плавное появление сообщений об ошибках
- ✅ **Поддержка темной темы**: Адаптированные цвета для ночного режима

### 5. **UX улучшения**
- ✅ **Валидация при потере фокуса**: Ошибки показываются после ухода из поля (blur)
- ✅ **Блокировка кнопки**: Кнопка "Продолжить" неактивна при невалидной форме
- ✅ **Визуальная обратная связь**: Изменение цвета кнопки (белая/синяя)
- ✅ **Метки полей**: Добавлены подписи с указанием обязательности (*)
- ✅ **Placeholder с подсказкой**: Например, "минимум 8 символов" для пароля
- ✅ **Уведомления**: Toast-уведомления при попытке отправки невалидной формы

### 6. **Обязательные поля**
- ✅ **Имя пользователя** - обязательно (минимум 3 символа)
- ✅ **Email** - обязательно (корректный формат)
- ✅ **Пароль** - обязательно (минимум 8 символов)
- ✅ **Подтверждение пароля** - обязательно (должно совпадать)
- ✅ **Согласие о возрасте** - обязательно (чекбокс)

## Технические детали

### Реактивная валидация
```javascript
// Объект состояния формы
const form = ref({
  email: '',
  username: '',
  password1: '',
  password2: '',
  ageAccept: false,
  adsAccept: false,
  isTeacher: false,
  teacher_ref_cod: ''
})

// Состояние "тронутых" полей
const touched = ref({
  username: false,
  email: false,
  password1: false,
  password2: false
})

// Computed-свойства для валидации
const isEmailValid = computed(() => {
  if (!form.value.email) return false
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  return emailRegex.test(form.value.email)
})

const isPasswordValid = computed(() => {
  return form.value.password1 && form.value.password1.length >= 8
})

const isPasswordMatch = computed(() => {
  return form.value.password1 === form.value.password2 && form.value.password2.length > 0
})

// Объект с текстами ошибок
const errors = computed(() => {
  return {
    username: !form.value.username ? 'Имя пользователя обязательно для заполнения' : 
              form.value.username.length < 3 ? 'Имя пользователя должно содержать минимум 3 символа' : '',
    email: !form.value.email ? 'Email обязателен для заполнения' : 
           !isEmailValid.value ? 'Введите корректный email адрес' : '',
    password1: !form.value.password1 ? 'Пароль обязателен для заполнения' : 
               form.value.password1.length < 8 ? 'Пароль должен содержать минимум 8 символов' : '',
    password2: !form.value.password2 ? 'Подтвердите пароль' : 
               !isPasswordMatch.value ? 'Пароли не совпадают' : ''
  }
})

// Общая валидность формы
const isValid = computed(() => {
  return (
    form.value.username &&
    form.value.username.length >= 3 &&
    isEmailValid.value &&
    isPasswordValid.value &&
    isPasswordMatch.value &&
    form.value.ageAccept
  )
})
```

### Отображение ошибок в template
```vue
<input
  v-model="form.username"
  type="text"
  class="custom-input"
  :class="{ 'error': errors.username && touched.username }"
  @blur="touched.username = true"
/>
<span v-if="errors.username && touched.username" class="error-message">
  {{ errors.username }}
</span>
```

## Стили

### Основные классы
- `.error-message` - текст ошибки под полем
- `.custom-input.error` - подсветка поля с ошибкой
- `.password-field` - контейнер для поля пароля с иконкой
- `.password-image` - иконка показа/скрытия пароля

### Поддержка темной темы
Все стили адаптированы для `.night__mode` класса.

## Тестирование

### Сценарии для проверки:
1. ✅ Попытка отправки пустой формы - показываются все ошибки
2. ✅ Ввод невалидного email - показывается ошибка формата
3. ✅ Ввод короткого пароля (< 8 символов) - показывается ошибка длины
4. ✅ Несовпадение паролей - показывается ошибка совпадения
5. ✅ Короткое имя (< 3 символов) - показывается ошибка длины
6. ✅ Попытка отправки без согласия о возрасте - кнопка неактивна
7. ✅ Валидная форма - кнопка активна, синего цвета
8. ✅ Показ/скрытие паролей - работают иконки глаза
9. ✅ Темная тема - корректное отображение

## Дальнейшие улучшения (опционально)

### Возможные расширения:
- [ ] Проверка сложности пароля (буквы, цифры, спецсимволы)
- [ ] Индикатор силы пароля (слабый/средний/сильный)
- [ ] Проверка email на существование (асинхронная валидация)
- [ ] Проверка имени пользователя на уникальность
- [ ] Дебаунсинг для асинхронных проверок
- [ ] Автокапча или reCAPTCHA
- [ ] Валидация номера телефона (если будет добавлено поле)
- [ ] Многоязычная поддержка сообщений об ошибках

## Безопасность

### Реализованные меры:
- ✅ Валидация на фронтенде (первая линия защиты)
- ✅ Минимальная длина пароля (8 символов)
- ✅ Защита от пустых значений
- ⚠️ **Важно**: Валидация на бэкенде обязательна!

### Рекомендации для бэкенда:
- Проверка всех полей на сервере
- Проверка уникальности email и username
- Хеширование паролей
- Rate limiting для защиты от брутфорса
- Email verification для подтверждения адреса
